@model mvc.Models.Client

@{
    ViewData["Title"] = "Client | Το Προφίλ μου";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-lg-6 col-md-8 col-sm-10">
            <div class="card shadow-lg rounded-3">
                <div class="card-header bg-primary text-white text-center">
                    <h2 class="mb-0">
                        <i class="fas fa-user-circle" style="font-size: 2.3rem; vertical-align: middle;"></i>
                        <span class="ms-2">Το Προφίλ μου</span>
                    </h2>
                </div>
                <div class="card-body">
                    <form asp-action="Edit">
                        <div class="mb-3">
                            <label for="ClientId" class="form-label">
                                <i class="fas fa-id-badge text-muted me-2"></i>
                                Το ID σου
                            </label>
                            <input type="text" asp-for="ClientId" class="form-control" readonly />
                        </div>
                        <div class="mb-3">
                            <label for="Afm" class="form-label">
                                <i class="fas fa-file-alt text-muted me-2"></i>
                                ΑΦΜ
                            </label>
                            <input type="text" asp-for="Afm" class="form-control" readonly />
                        </div>
                        <div class="mb-3">
                            <label for="PhoneNumber" class="form-label">
                                <i class="fas fa-phone-alt text-muted me-2"></i>
                                Τηλέφωνο
                            </label>
                            <input type="text" asp-for="PhoneNumber" class="form-control" readonly />
                        </div>
                        <div class="text-center">
                            <button type="submit" class="btn btn-primary px-4">
                                <i class="fas fa-edit me-2"></i> Επεξεργασία
                            </button>
                            <button type="button" class="btn btn-secondary px-4 ms-3" onclick="window.location.href='@Url.Action("Index", "Client")'">
                                <i class="fas fa-arrow-left me-2"></i> Πίσω στην αρχική
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" />
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
